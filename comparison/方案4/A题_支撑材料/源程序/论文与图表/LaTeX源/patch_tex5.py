# -*- coding: utf-8 -*-
"""压缩摘要至一页内（规范第三条：摘要不超过一页）"""
import io
import os

P = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'paper.tex')
s = io.open(P, encoding='utf-8').read()

REP = [
    (r"""\textbf{可信度复核}（本文的重点工作之一）：数值上通过了谱基恒等式、平凡解与零通量守恒、
网格与时间步收敛（问题 3 的 $t_{dry}$ 在 43—81 自由度间仅变化 0.2 s）、与独立编写的
单元中心有限体积解对拍，以及 Radau/BDF/LSODA 三积分器互校；另与一份完全独立的实现
逐格比对，表 1—表 6 全部 216 个输出格子在四位小数上一致；
并新建二维轴对称模型复核了几何假设、独立复现了非均匀收缩模型。
模型形式上，本文指出题面
三处扩散系数的指数为\textbf{分式} $\ee^{-0.89/C}$ 而非乘积 $\ee^{-0.89C}$（二者在 $C=1$ 处才相等，
在干燥区间内 $D$ 的差别达 3 个数量级，误读将使问题 3 的时长由 57.17 h 变为约 16.4 h）；
并量化了干基守恒形式、收缩假设、环境数据处理与判据口径各自带来的偏差。""",
     r"""\textbf{可信度复核}：数值层面通过谱基恒等式、守恒律、网格与时间步收敛（问题 3 的
$t_{dry}$ 在 43—81 自由度间仅变化 0.2 s）、独立有限体积对拍与三积分器互校，
并与另一份独立实现逐格比对，表 1—表 6 全部 216 个格子在四位小数上一致；另建二维轴对称
模型复核几何假设、独立复现了非均匀收缩模型。模型层面指出题面三处扩散系数的指数为
\textbf{分式} $\ee^{-0.89/C}$ 而非乘积 $\ee^{-0.89C}$（二者仅在 $C=1$ 处相等，干燥区间内
$D$ 相差 3 个数量级，误读会使问题 3 的时长由 57.17 h 变为约 16.4 h），
并量化了守恒形式、收缩假设、环境处理与判据口径的偏差。"""),
    (r"""\textbf{求解方面}，采用偶数 Legendre 多项式 $P_{2j}(2\sigma-1)$ 为谱基，使质量矩阵具有解析
对角形式 $M_{jj}=1/[2(4j+1)]$（实测与解析偏差 $2\times10^{-15}$，条件数仅约 $4N$）；
柱心 $1/\sigma$ 奇异由弱形式自动消解；表面 Robin 条件以自然边界条件进入方程，
半离散格式因而精确满足守恒律 $\frac{\dd}{\dd t}\int\rho c_pT\sigma\ud\sigma=-\frac{h}{R}(T_s-T_\infty)$
与 $\frac{\dd}{\dd t}\int C\sigma\ud\sigma=-\frac{h_m}{R}(C_s-C_\infty)$。
针对表面薄边界层引起的谱振铃失稳，采用分级谱元（最外层单元专门覆盖边界层）；
时间方向用自适应隐式 BDF/Radau，终止时刻由连续事件函数的根查找确定。""",
     r"""\textbf{求解方面}，以偶数 Legendre 多项式 $P_{2j}(2\sigma-1)$ 为谱基，质量矩阵解析对角
$M_{jj}=1/[2(4j+1)]$（与解析值偏差 $2\times10^{-15}$，条件数约 $4N$）；柱心 $1/\sigma$
奇异由弱形式自动消解；表面 Robin 条件以自然边界进入方程，离散格式因而精确满足守恒律
$\frac{\dd}{\dd t}\int\rho c_pT\sigma\ud\sigma=-\frac{h}{R}(T_s-T_\infty)$。
针对表面薄边界层引起的谱振铃失稳采用分级谱元；时间方向用自适应隐式 BDF/Radau，
终止时刻由连续事件函数根查找确定。"""),
]
n = 0
for a, b in REP:
    if a in s:
        s = s.replace(a, b, 1)
        n += 1
    else:
        print('NOT FOUND:', a[:40])
io.open(P, 'w', encoding='utf-8').write(s)
print('replaced', n)
